const prompt = require("prompt-sync")()

let numeroinicial = Number(prompt("Qual número inicial? "))
let multiplicador = Number(prompt("Qual número multiplicador?"))
let divisor = Number(prompt("Qual número divisor"))

let